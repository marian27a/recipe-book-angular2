/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BetterHaighlitDirective } from './better-haighlit.directive';

describe('BetterHaighlitDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHaighlitDirective();
    expect(directive).toBeTruthy();
  });
});
